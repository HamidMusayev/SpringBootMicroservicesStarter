server:
  port: 8888  # Default port for Config Server

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/HamidMusayev/SpringBootMicroservicesStarter.git  # Replace with your actual config-repo URL
          searchPaths: '{application}'
          cloneOnStart: true
